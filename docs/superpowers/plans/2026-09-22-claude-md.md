# CLAUDE.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar um `CLAUDE.md` que importa o `AGENTS.md` e acrescenta a prioridade do produto e os sinais do log de produção, depois de corrigir quatro defasagens do `AGENTS.md`.

**Architecture:** Só documentação. O `AGENTS.md` continua a fonte de verdade das regras; o `CLAUDE.md` o importa com `@AGENTS.md` (o Claude Code expande a importação ao carregar) e acrescenta duas seções. Nenhum código, configuração ou teste muda.

**Tech Stack:** Markdown; verificação com `grep`, `git diff --check` e a suíte pytest existente.

**Spec:** `docs/superpowers/specs/2026-09-22-claude-md-design.md`

## Global Constraints

- Todo texto em português, no tom do `AGENTS.md` (frases diretas, sem marketing).
- O `CLAUDE.md` não repete nenhuma regra do `AGENTS.md`; só importa com `@AGENTS.md`.
- No `AGENTS.md`, só as correções listadas na Task 1; o resto fica byte a byte igual.
- Nenhum segredo, token ou URL privada no texto.
- O Python do projeto roda por `.\.venv\Scripts\python.exe` (Windows).

---

### Task 1: Corrigir as defasagens do `AGENTS.md`

**Files:**
- Modify: `AGENTS.md:72-73` (`sources/`), `AGENTS.md:76-77` (`preco/`), `AGENTS.md:85-89` (`painel/`, `data/`, `scripts/`), `AGENTS.md:137-138` (invariante de rede), `AGENTS.md:141-142` (fim dos invariantes), `AGENTS.md:174-183` (renovação da cotação)

**Interfaces:**
- Consumes: nada.
- Produces: `AGENTS.md` atualizado, que a Task 2 importa. A Task 2 não depende de nenhum texto específico daqui.

Cada passo é uma substituição exata. Use o Edit com o texto "antes" como `old_string`.

- [ ] **Step 1: Confirmar que os fatos novos existem no código**

Run:
```bash
ls tf2price/efeitos/coletor.py tf2price/efeitos/arte.py tf2price/sources/bcb.py tf2price/preco/referencia.py scripts/fetch_cosmeticos.py tf2price/data/cosmeticos.json
grep -n "class PtaxSobDemanda\|def obter\|def renovar" tf2price/painel/ptax.py
grep -n "cotacaoVenda" tf2price/sources/bcb.py
grep -n "def montar_referencia" tf2price/preco/referencia.py
```
Expected: os seis arquivos listados sem erro; `class PtaxSobDemanda`, `def obter` e `def renovar` em `ptax.py`; `cotacaoVenda` em `bcb.py`; `def montar_referencia` em `referencia.py`. Se algo faltar, pare e avise: a spec partiu de um fato que mudou.

- [ ] **Step 2: `sources/` passa a citar o Banco Central**

Antes:
```markdown
- `tf2price/sources/`: clientes da Steam e backpack.tf, interpretação das
  respostas, limites de requisição e backoff.
```
Depois:
```markdown
- `tf2price/sources/`: clientes da Steam, backpack.tf e Banco Central (PTAX),
  interpretação das respostas, limites de requisição e backoff.
```

- [ ] **Step 3: `preco/` passa a citar a chave de referência**

Antes:
```markdown
- `tf2price/preco/`: serialização, repositórios e retrato compartilhado de preço
  com validade, idade e comportamento degradado.
```
Depois:
```markdown
- `tf2price/preco/`: serialização, repositórios, retrato compartilhado de preço
  com validade, idade e comportamento degradado, e a chave de referência em
  dinheiro (`referencia.py`).
```

- [ ] **Step 4: Mapa ganha `efeitos/`; `data/` e `scripts/` ficam completos**

Antes:
```markdown
- `tf2price/painel/`: aplicação FastAPI, composição de dependências, rotas,
  autenticação, templates Jinja/HTMX e trabalho de aquecimento em segundo plano.
- `tf2price/data/`: `effects.json` e artes WebP empacotadas com a aplicação.
- `scripts/`: geração explícita de dados, hoje iniciada por
  `scripts/fetch_effects.py`.
```
Depois:
```markdown
- `tf2price/painel/`: aplicação FastAPI, composição de dependências, rotas,
  autenticação, templates Jinja/HTMX e trabalho de aquecimento em segundo plano.
- `tf2price/efeitos/`: arte dos efeitos. `coletor.py` é a coleta única, feita
  pelo navegador do dono porque a backpack.tf recusa servidor; `arte.py` liga o
  nome do efeito ao WebP empacotado. A arte nunca é buscada ao vivo.
- `tf2price/data/`: `effects.json`, `cosmeticos.json` e artes WebP empacotadas
  com a aplicação.
- `scripts/`: geração explícita de dados: `scripts/fetch_effects.py` gera
  `effects.json` e `scripts/fetch_cosmeticos.py` gera `cosmeticos.json`.
```

- [ ] **Step 5: Invariante de rede cobre a PTAX**

Antes:
```markdown
- Nenhum import ou teste pode acessar a rede. Rotas não devem bloquear à espera
  da renovação da cotação.
```
Depois:
```markdown
- Nenhum import ou teste pode acessar a rede. Rotas não devem bloquear à espera
  da renovação da cotação ou da PTAX.
```

- [ ] **Step 6: Novo invariante da chave de referência**

Antes:
```markdown
- Compatibilidade entre SQLite nos testes e PostgreSQL em produção é requisito;
  não dependa de permissividade, coerção ou semântica exclusiva de um dialeto.

## Banco de dados e transações
```
Depois:
```markdown
- Compatibilidade entre SQLite nos testes e PostgreSQL em produção é requisito;
  não dependa de permissividade, coerção ou semântica exclusiva de um dialeto.
- Toda conta de troca ("N chaves", saída pela troca, prêmio, resultado da
  varredura) usa a chave de referência: o dólar da chave na backpack.tf vezes a
  PTAX de venda do Banco Central. O preço da chave na Steam só converte as
  listagens que a Steam devolve em dólar; não o reintroduza em conta de troca.

## Banco de dados e transações
```

- [ ] **Step 7: Regra de renovação pelo fundo vale para a PTAX**

Antes:
```markdown
`CotacaoSobDemanda.obter` consulta apenas memória e banco: não acessa a rede. A
renovação da cotação pertence a `CotacaoSobDemanda.renovar` e é executada
exclusivamente pelo trabalho de fundo; rotas respondem com a cotação antiga ou
com o estado ainda indisponível em vez de renová-la.

Essa regra de renovação em segundo plano é específica da cotação. Outras buscas
remotas existentes, especialmente retratos via `Retratos.obter`, podem ocorrer
sincronicamente em uma rota. Mesmo nesses caminhos, conclua a leitura do banco e
devolva a conexão ao pool antes do I/O externo; abra outra transação curta apenas
depois da resposta, se for necessário persistir o resultado.
```
Depois:
```markdown
`CotacaoSobDemanda.obter` e `PtaxSobDemanda.obter` consultam apenas memória e
banco: não acessam a rede. A renovação pertence a `renovar` de cada uma e é
executada exclusivamente pelo trabalho de fundo; rotas respondem com o valor
antigo ou com o estado ainda indisponível em vez de renová-lo.

Essa regra de renovação em segundo plano é específica da cotação e da PTAX.
Outras buscas remotas existentes, especialmente retratos via `Retratos.obter`,
podem ocorrer sincronicamente em uma rota. Mesmo nesses caminhos, conclua a
leitura do banco e devolva a conexão ao pool antes do I/O externo; abra outra
transação curta apenas depois da resposta, se for necessário persistir o
resultado.
```

O segundo parágrafo foi refluído para caber em 80 colunas; só "e da PTAX" é palavra nova nele.

- [ ] **Step 8: Verificar o diff**

Run:
```bash
git diff --check AGENTS.md
git diff --stat AGENTS.md
```
Expected: `--check` sem saída; `--stat` mostra só `AGENTS.md`. Leia `git diff AGENTS.md` e confirme que só os trechos dos Steps 2–7 mudaram.

- [ ] **Step 9: Commit**

```bash
git add AGENTS.md
git commit -F - <<'EOF'
AGENTS.md alcança o código: efeitos, PTAX e chave de referência

O mapa não tinha tf2price/efeitos/ nem o cliente do Banco Central, scripts/
citava só fetch_effects.py, e faltava a decisão de 22/09: conta de troca usa
a chave de referência, nunca o preço da chave na Steam. A regra de renovação
só pelo fundo passa a nomear a PTAX, que segue o mesmo desenho da cotação.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013JDCiFFVoni7jxr2QsMEf7
EOF
```

---

### Task 2: Criar o `CLAUDE.md`

**Files:**
- Create: `CLAUDE.md`

**Interfaces:**
- Consumes: `AGENTS.md` na raiz (Task 1), importado por `@AGENTS.md`.
- Produces: nada que outra task use.

- [ ] **Step 1: Confirmar cada prefixo de log e cada fato da tabela**

Run:
```bash
grep -rhoE --include=*.py "\[(partida|superadmin|aquecimento|renovo|sob-demanda|ptax|retrato|varredura)\]" tf2price | sort -u
grep -n '_aquece("' tf2price/painel/app.py
grep -n "CALMA_APOS_429 =" tf2price/preco/retrato.py
grep -n "PtaxSobDemanda" tf2price/painel/ptax.py
```
Expected: exatamente oito linhas, uma por prefixo; `_aquece("cotação"`, `_aquece("PTAX"` e `_aquece("índice"`; `CALMA_APOS_429 = timedelta(minutes=5)`; `[sob-demanda] PtaxSobDemanda` existe em `ptax.py`. Se algum não bater, ajuste a linha correspondente da tabela ao que o código faz e registre a diferença no commit.

- [ ] **Step 2: Escrever o `CLAUDE.md`**

Conteúdo completo:
```markdown
# CLAUDE.md

@AGENTS.md

## Prioridade do produto

O produto é a tela de consulta de um Unusual: decidir uma compra em segundos —
quanto vale na troca, quanto alguém paga agora, e há quanto tempo o preço de
referência não é atualizado.

A varredura (`tf2price/varredura/`) complementa a consulta, não a substitui. Ela
sai do mesmo IP, então nunca pode causar 429 para a consulta: gasta o mínimo de
requisições, espera a calma ligada por outros, cede a vez e pausa ao primeiro
429.

Proposta de varredura, worker, alerta ou ranking do mercado inteiro precisa ser
justificada, não presumida. O caminho padrão é melhorar a consulta e a
honestidade dos números que ela mostra.

## Sinais no log de produção (Railway)

As linhas que o painel imprime começam por um prefixo entre colchetes. Ao
investigar um problema em produção, procure primeiro por elas.

| Prefixo | Quando aparece | O que significa |
|---|---|---|
| `[partida]` | subida com o banco sem usuário | link de convite de administrador, válido por 24 h |
| `[superadmin]` | subida | `SUPERADMIN` ausente ou não é admin ativo: ninguém promove nem rebaixa. Esperado só na primeira subida, antes de a conta existir |
| `[aquecimento]` | subida | carga de cotação, índice e PTAX, com o tempo gasto. Se diz "ok em ~2s" e a página ainda demora, a causa é a partida do contêiner, não a carga |
| `[renovo]` | ciclo de fundo | a renovação da cotação ou da PTAX falhou; a tela segue com o dado velho e a idade à vista |
| `[sob-demanda]` | falha de busca | origem, erro e espera até a nova tentativa. `PtaxSobDemanda` é o que procurar quando a tela diz que a PTAX não carregou |
| `[ptax]` | ciclo de fundo | PTAX nova guardada no banco |
| `[retrato]` | consulta ou varredura | a Steam limitou (429) a página do item; calma de 5 min |
| `[varredura]` | fundo | 429 com pausa, resumo da rodada, rodada interrompida por reinício, erro do agendador |
```

- [ ] **Step 3: Verificar**

Run:
```bash
git diff --check
git status --short
.\.venv\Scripts\python.exe -m pytest
```
Expected: `--check` sem saída; `git status` mostra só `?? CLAUDE.md`; pytest termina com `passed` e nenhuma falha (a suíte não muda, o checklist do `AGENTS.md` pede a execução mesmo assim). Se algum teste falhar, pare: nada deste plano toca código, então a falha é pré-existente e precisa ser relatada, não consertada aqui.

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -F - <<'EOF'
CLAUDE.md importa o AGENTS.md e diz o que o log de produção quer dizer

O Claude Code lê CLAUDE.md, não AGENTS.md. Em vez de uma segunda cópia das
regras, importa a primeira e acrescenta a prioridade do produto (a consulta
é o produto; a varredura nunca causa 429 para ela) e a tabela dos prefixos
de log do Railway.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013JDCiFFVoni7jxr2QsMEf7
EOF
```

- [ ] **Step 5: Marcar a spec como implementada**

Padrão do repositório (commit `daa1f64`): trocar a linha de status da spec. Em `docs/superpowers/specs/2026-09-22-claude-md-design.md`:

Antes:
```markdown
**Status:** aprovado, não implementado
```
Depois:
```markdown
**Status:** implementado
```

Commit:
```bash
git add docs/superpowers/specs/2026-09-22-claude-md-design.md
git commit -F - <<'EOF'
Spec do CLAUDE.md implementada

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013JDCiFFVoni7jxr2QsMEf7
EOF
```
